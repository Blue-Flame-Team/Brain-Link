# تقرير المشروع الشامل: تطبيق BrainLink 🧠🔗

## 1. نظرة عامة على المشروع (Project Overview)
تطبيق **BrainLink** هو منصة تواصل اجتماعي وتعليمي مخصصة للمطورين وطلاب علوم الحاسب التقنية. يهدف التطبيق إلى خلق بيئة تفاعلية تسمح بمشاركة المعرفة، تبادل الأكواد البرمجية، عقد جلسات تعليمية وتوفير مكتبة غنية بالمصادر التقنية.
التطبيق مبني بالكامل باستخدام إطار عمل **Flutter** لتوفير تجربة مستخدم سلسة على منصات المحمول، ويعتمد على **Firebase** كخادم خلفي (Backend) لإدارة قواعد البيانات والتوثيق والملفات.

## 2. التقنيات والأدوات المستخدمة (Tech Stack & Architecture)
**البيئة وتطوير الواجهات:**
- إطار العمل: **Flutter (Dart)** - لتطوير واجهات المستخدم بناءً على هيكل مكونات حديث (Material 3).
- إدارة الحزم: `pubspec.yaml` بمجموعة من الحزم الحديثة مثل `lottie`، `intl`، `share_plus`.

**الخدمات السحابية (Firebase):**
- **Firebase Authentication**: لتسجيل الدخول وإنشاء حسابات المستخدمين.
- **Cloud Firestore**: قاعدة البيانات السحابية المركزية لتخزين المنشورات، الجلسات، الرسائل والمكتبة (تخزين NoSQL).
- **Firebase Storage / Realtime Database**: للتعامل مع تحميل ومشاركة الملفات سواء في الدردشات أو المكتبة.

**التخزين المحلي (Local Storage & Offline Support):**
يُعد التخزين غير المتصل بالإنترنت (Offline Mode) إحدى أهم ميزات التطبيق. وتتم إدارتها عبر مسارين:
- `sqflite`: لإدارة قاعدة بيانات محلية باستخدام لغة SQL، خُصصت لحفظ بيانات المستخدم الموثق وحفظ "المنشورات المفضلة" (Favorites).
- `shared_preferences`: لحفظ الجلسات، المنشورات، والمكتبة والدردشات مؤقتاً كبيانات JSON لتسريع عمل التطبيق وعرض المحتوى حتى عند غياب الإنترنت.

---

## 3. شرح هيكل الملفات والأكواد بالتفصيل الممل (Detailed Code Structure)

تم تنظيم المشروع باتباع بنية هيكلية واضحة (Folder Structure) داخل المجلد `lib/`:
```
lib/
├── helpers/       # الملفات المساعدة (التخزين المحلي، إدارة الملفات)
├── model/         # نماذج البيانات (هيكلة الكائنات مثل Session, Post, User)
├── navigation/    # إدارة التنقل (Routing) بين الشاشات
├── screens/       # واجهات المستخدم مقسمة لمجلدات (الرئيسية، الدردشة، المكتبة وغيرها)
├── services/      # الخدمات المركزية المربوطة بالسحابات والتخزين (Auth, Firestore, Local)
└── main.dart      # نقطة دخول التطبيق
```

### أولاً: نقطة الدخول (main.dart)
ملف نقطة الدخول يبدأ بتهيئة `WidgetsFlutterBinding` ثم تهيئة حساب `Firebase.initializeApp()`.
نقطة هامة جداً في الأكواد هنا هي تنشيط قدرة الـ Firebase على التخزين السحابي المحلى عبر:
```dart
FirebaseFirestore.instance.settings = const Settings(
  persistenceEnabled: true,
  cacheSizeBytes: Settings.CACHE_SIZE_UNLIMITED,
);
```
وكذلك يعتمد التطبيق على كائن `WidgetsBindingObserver` لمراقبة دورة حياة التطبيق (App Lifecycle). بفضله، بمجرد أن يغلق المستخدم الشاشة (Paused) يتغير حالته (`isOnline`) إلى `false` في فايربيس، وإذا فتح التطبيق تعود `true` لتفعيل خدمة "متصل الآن".

### ثانياً: قسم الخدمات (Services)

#### 1. خدمة التوثيق `auth_service.dart`
أُسندت إليها مهمة التواصل مع `FirebaseAuth`.
عند التسجيل بدالة `signUp()`، لا يقومแค่ بإنشاء حساب بالايميل والباسورد وحسب، ولكنه يأخذ إسم المستخدم (Name) ويحدث به `DisplayName`، وفي حال النجاح يضيف وثيقة داخل كوليكشن `users` بالفايرستور ليحفظ البيانات كالتالي:
```dart
await _db.collection('users').doc(uid).set({
  'fullName': name,
  'email': email,
  'createdAt': FieldValue.serverTimestamp(),
});
```

#### 2. خدمة فايرستور `firestore_service.dart`
هذا الملف هو العصب المغذي لكل التطبيق، فهو يحتوي على عشرات الدوال للتعامل مع قاعدة البيانات:
- **دوال Streams (Live Data):** دوال تعيد `Stream<List<T>>` لتغذية الـ UI لحظياً (Realtime) للـ `Posts`, `Sessions`, `LibraryItems`. يتم جلب المنشورات بالترتيب الزمني وعمل (Mapping) لتحويل كل وثيقة من Firestore إلى Model Object للبيانات.
- **نظام التفاعل:** توجد دالة `toggleLikePost` تعتمد على `FieldValue.arrayUnion` و `FieldValue.arrayRemove` لتخزين إعجابات المستخدمين بأمان وتجنب التكرار.
- **نظام الدردشة (Chats):** دالة `getChats` تبحث عن مساحات الدردشة التي يشارك فيها المستخدم عن طريق `arrayContains`، و دالة `getOrCreateChat` تقوم بتوليد معرّف دردشة مميز و موحد لغرف الدردشة الثنائية عن طريق تجميع الـ UIDs وترتيبهم أبجدياً `participants.join('_')` ضماناً لعدم تكرار غرف المحادثات لنفس الشخصين.

#### 3. التخزين المؤقت للبيانات `local_storage_service.dart`
تم إنشاء دوال مثل `savePost`, `saveSession`, `saveLibraryItem` بحيث تقوم بتحويل الـ Models إلى صيغة JSON وإضافتها داخل `SharedPreferences` باستخدام Strings List. يعتمد المنطق هنا على استخدام هيكل الـ `Map` حيث الـ Key هو مُعرف الـ ID لمنع تكرار البيانات المحفوظة.

### ثالثاً: إدارة قواعد البيانات المحلية (Helpers) `db_helper.dart`
تم إنشاء كلاس `DbHelper` باتباع نمط (Singleton Pattern) ليتم إنشاء قاعدة بيانات `brainlink.db` مرة واحدة.
تم الإعداد بإصدار (`version: 2`) وتم تنفيذ إنشاء جدولين (`CREATE TABLE`):
1. الـ `users`: لحفظ بيانات المستخدم الأساسية كنسخة محفظة.
2. الـ `favorites`: لتخزين البوستات (المنشورات) المفضلة بشكل Offline. وبذلك، حتى دون إنترنت يمكن للمستخدم استرجاع وقراءة المنشورات المفضلة. يتم تحويل الليست الخاصة بالإعجابات لـ String مفصول بفواصل (comma-separated).

### رابعاً: النماذج (Models) `app_models.dart` وغيرها
النماذج مبنية بنظام الـ Data Classes: كـ `Post`, `Session`, `LibraryItem`, `ChatItem`.
تحتوي على `factory fromMap` لتحويل الـ Map القادم من فايربيس أو التخزين المحلى لكائن برمجي يمكن للنظام فهمه، وعكس العملية عن طريق `toMap`.

### خامساً: واجهات المستخدم والشاشات (Screens & Tabs)
يحتوي التطبيق على هيكل Navigation سفلي `main_layout.dart` مزود بـ 5 تبويبات (Tabs). تم استخدام لون موحد كمرجع `Color(0xFF5E35B1)` وهو اللون البنفسجي العميق.

#### 1. الرئيسية (`HomeTab.dart`)
- **الجلسات النشطة:** تقرأ بث مباشر `Stream` للجلسات وتعرض فقط ما هو مكتوب له `isLive = true` بطريقة السحب الأفقي (`ListView.builder` horizontal).
- **مجتمع المنشورات (Posts):** واجهة تقرأ كائنات البوست ولها قدرات تفاعل للإعجاب، أو الإضافة للمفضلة. إضافة للمقارنة من خلال `DbHelper` للتأكد أن البوست مضاف محلياً وتغيير لون الزر (Favorite/Unfavorite). ويوجد Notification System يُرسل إشعاراً لصاحب المنشور عند إضافته كمفضلة عند مستخدم آخر.

#### 2. المكتبة (`LibraryTab.dart`)
واجهة غنية مقسمة لأقسام ثابتة (الكتب التقنية، كورسات، ملخصات، أكواد، إلخ). بالضغط على أي منها ينقلنا لـ `LibraryCategoryScreen` ويقوم بفرزها من الداتا بيز. يمكن للمستخدم إضافة مواد بالمكتبة عن طريق `AddLibraryScreen` والمزود بإمكانيات رفع وتحديد تصنيف.

#### 3. الدردشات (`ChatTab.dart`)
تعرض شاشة الدردشة قائمة بمن تتحدث معهم. يتم قراءة الـ Stream ليتم وضع علامة النقطة الخضراء (isOnline) باستخدام `getUserPresence`. يحتوي الواجهة على أنظمة مراجع بصرية مثل الأيقونات لو كان هنالك مرفق بالرسالة وصورة الشخص كحرف أول أو صورته.

#### 4. الجلسات (`SessionsTab.dart`) والبروفايل (`ProfileTab.dart`) وغيرها أُعطيت تصميم زجاجي ورسومات Lottie في بعض أجزائها. الملف الشخصي يتيح التعديل وأيضاً يقرأ دور ومسمى المستخدم الوظيفي (Role) المُعطى وقت التسجيل.

## 4. ملخص سير عمل التطبيق (The Application Flow)
1. يفتح التطبيق `main.dart` ويتصل بالـ Firebase المحلي والشبكي ويسهر على استماع دورة الاستخدام.
2. ينتقل لشاشات الدخول عبر `RouterGenerator`.
3. بفضل `AuthService` يتعرف على المستخدم ويوجهه إلى الـ `MainLayout`.
4. داخل التطبيق تندمج البيانات الواردة لحظياً (Streams) في الواجهات ذات التصاميم الجميلة مع معالجة غير متصلة بالإنترنت بالخلفية لضمان عمل كافة الوظائف.

---

**ملاحظة لمطوّر المشروع:** 
هذا التقرير يستعرض بنية التطبيق بالكامل ومراحل الترابط بين الواجهة (Flutter) والمحرك الخلفي (Firebase) مع الاعتماد الكامل على (Best Practices) في وضع الخدمات وفصل مسؤولية الـ Business Logic في Service و Helpers عن الـ UI.
